<!DOCTYPE html>
<html lang="en-US">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/style.css?181">
    <script src="/script.js?56"></script>
</head>

<body>
    {% if nav %}
    <nav>
        {% if user.account_opt is not none %}
        <a href="/user/{{ user.account_opt.username }}">{{ user.account_opt.username }}</a>
        {% else %}
        <a href="/login">log in</a>
        {% endif %}
    </nav>
    {% endif %}
    <header>
        <h1><a href="/">{{ title }}</a></h1>
    </header>
    {% filter indent(4) %}
    {%+ block main %}
    {% endblock +%}
    {% endfilter %}
</body>

</html>
