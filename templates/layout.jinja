<!DOCTYPE html>
<html lang="en-US">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/style.css?135">
    <script src="/script.js?41"></script>
</head>


<body>
    {% if nav %}
    <nav>
        {% if user.account %}
        <a href="/user/{{ user.account.username }}">{{ user.account.username }}</a>
        {% else %}
        <a href="/login">log in</a>
        {% endif %}
    </nav>
    {% endif %}
    <header>
        <h1><a href="/">{{ title }}</a></h1>
    </header>
    <main>
        {% block main %}
        {% endblock %}
    </main>
</body>

</html>
