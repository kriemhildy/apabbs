{
  "name": "apabbs",
  "private": true,
  "scripts": {
    "chrome:install": "npm exec -- @puppeteer/browsers install chrome@stable && script/chrome-cleanup.sh",
    "chrome": "npm exec -- @puppeteer/browsers launch chrome@stable --dumpio --"
  }
}
